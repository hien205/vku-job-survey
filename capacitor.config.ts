import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hien205.vkujobsurvey',
  appName: 'VKU Student Job Survey',
  webDir: 'dist',
};

export default config;
